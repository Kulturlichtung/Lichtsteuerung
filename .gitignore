.claude/
.idea/
*.iml
__pycache__/
*.pyc

# chart.js + chartjs-plugin-annotation, fetched onto the Pi at setup/
# update time (README "Projekt-Code auf den Pi bringen"), not committed
# -- see CLAUDE.md's "Tuning web UI" section for why.
beat-detector/static/vendor/
